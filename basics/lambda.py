# 对于一些简单的数字表达式的计算返回时，没有必要自己去定义函数，我们可以使用 lambda 来完成
def true(): return True


true()

# 可以简写为
lambda: True

# filter()
list1 = [1, 2, 3, 4, 5, 6, 7, 8]

# list[filter(lambda x: x > 2, list1)]

# map()

# reduce()

# zip()
